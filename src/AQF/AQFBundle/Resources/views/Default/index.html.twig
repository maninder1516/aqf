{% extends 'base.html.twig' %}

{% block body %}
<!-- Content Header (Page header) -->
<section class="content-header">
  <h1>Missions</h1>   
  <ol class="breadcrumb">
    <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
    <li class="active">Missions</li>
  </ol>
</section>

<!-- Main content -->
<section class="content">
	<div class="row">
        <div class="col-xs-12">
        	<div class="box">
        		<!-- / box-header -->
        		<div class="box-header">
	              <h3 class="box-title">Data Table With Full Features</h3>
	              <a class="btn btn-info pull-right" title="Add mission" alt="Add mission" href="{{ path('aqf_addedit',{'mid': 0}) }}">&nbsp; Add New</a>
	            </div>
	            <!-- /.box-header -->
	            <div class="box-body">
	            	<table id="example1" class="table table-bordered table-striped">
	            		<thead>
			                <tr>
			                  <th>Username</th>
			                  <th>Service Date</th>
			                  <th>Product Name</th>
			                  <th>Vendor Name</th>
			                  <th>Vendor Email</th>
			                </tr>
		                </thead>
		                <tbody>
	                		{% for mission in missions %}
			                <tr>
			                  <td> {{ mission.client }}</td>
			                  <td> {{ mission.serviceDate|date('d-m-Y') }}</td>
			                  <td> {{ mission.productName }}</td>
			                  <td> {{ mission.vendorName }}</td>
			                  <td> {{ mission.vendorEmail }}</td>
			                </tr>
			            	{% endfor %}
			            </tbody>
	            	</table>
	            </div>
        	</div>
        	<!-- /.box -->
        </div>
        <!-- /.col -->
        <!-- page script -->
        <script>
        //   $(function () {
        //     $('#example1').DataTable()
        //     $('#example2').DataTable({
        //       'paging'      : true,
        //       'lengthChange': false,
        //       'searching'   : false,
        //       'ordering'    : true,
        //       'info'        : true,
        //       'autoWidth'   : false
        //     })
        //   });
        </script>
    </div>
</section>
<!-- /.Main content -->

{% endblock %}
